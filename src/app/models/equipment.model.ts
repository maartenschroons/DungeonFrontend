export class Equipment {
    constructor(public index: number, public name:string, public equipment_category: string, public category_range: string,public weapon_range: string, public weight: number){}
}
